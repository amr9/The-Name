# Anything that should never end up in a build context.
node_modules
server/node_modules
dist
.git
.gitignore
.claude
*.local
.env
.env.*
server/.env

# Local SQLite database — the container gets its own on a volume.
server/data
*.db
*.db-wal
*.db-shm
Dockerfile
server/Dockerfile
docker-compose.yml
.dockerignore
*.md
*.pdf
.DS_Store
